# Design System — tokens (brand: meridian-atlas-ui)
> GENERATED by @ds/context — DO NOT EDIT. Source hash sha256:b95446232cde065d. Regenerate: pnpm --filter @ds/context build

This system is closed-world: every legal component is enumerated in registries/components-index.json and every legal token in registries/tokens-index.json. Anything not in those registries is provably fabricated — do not emit it.

44 tokens. Values below are RESOLVED for the `meridian-atlas-ui` brand (source: examples/legacy-ds-retrofit/brand/meridian-atlas-ui.json); a different brand build re-emits this file with its own values. Reference tokens only as `var(--ds-…)` — never paste resolved values into code.

Tier rules: semantic tokens (intent-named) are the general vocabulary; component tokens (`--ds-<component>-*`) are theming hooks that belong to their component ONLY — never borrow another component's hooks. Space tokens are legal only in margin/padding/gap/inset; box dimensions use `--ds-size-*`.

## Semantic tier

### atlas (7)

| Token | CSS variable | Value | Description |
| --- | --- | --- | --- |
| `atlas.blue` | `--atlas-blue` | `#1a56db` | Retrofit-synthesized from styles/atlas.css:11 (declared as `--atlas-blue` in `:root`). |
| `atlas.blue.dark` | `--atlas-blue-dark` | `#1e429f` | Retrofit-synthesized from styles/atlas.css:12 (declared as `--atlas-blue-dark` in `:root`). |
| `atlas.gray.100` | `--atlasGray100` | `#f3f4f6` | Retrofit-synthesized from styles/atlas.css:14 (declared as `--atlasGray100` in `:root`). |
| `atlas.gray.300` | `--atlasGray300` | `#d1d5db` | Retrofit-synthesized from styles/atlas.css:15 (declared as `--atlasGray300` in `:root`). |
| `atlas.gray.50` | `--atlasGray50` | `#f9fafb` | Retrofit-synthesized from styles/atlas.css:13 (declared as `--atlasGray50` in `:root`). |
| `atlas.gray.700` | `--atlasGray700` | `#374151` | Retrofit-synthesized from styles/atlas.css:16 (declared as `--atlasGray700` in `:root`). |
| `atlas.gray.900` | `--atlasGray900` | `#111827` | Retrofit-synthesized from styles/atlas.css:17 (declared as `--atlasGray900` in `:root`). |

### brand (6)

| Token | CSS variable | Value | Description |
| --- | --- | --- | --- |
| `brand.danger` | `--brand-danger` | `#b91c1c` | Retrofit-synthesized from styles/atlas.css:30 (declared as `--brand-danger` in `:root`). |
| `brand.danger.bg` | `--brand-danger-bg` | `#fef2f2` | Retrofit-synthesized from styles/atlas.css:31 (declared as `--brand-danger-bg` in `:root`). |
| `brand.success` | `--brand-success` | `#047857` | Retrofit-synthesized from styles/atlas.css:34 (declared as `--brand-success` in `:root`). |
| `brand.success.bg` | `--brand-success-bg` | `#ecfdf5` | Retrofit-synthesized from styles/atlas.css:35 (declared as `--brand-success-bg` in `:root`). |
| `brand.warning` | `--brand-warning` | `#92400e` | Retrofit-synthesized from styles/atlas.css:32 (declared as `--brand-warning` in `:root`). |
| `brand.warning.bg` | `--brand-warning-bg` | `#fffbeb` | Retrofit-synthesized from styles/atlas.css:33 (declared as `--brand-warning-bg` in `:root`). |

### btn (2)

| Token | CSS variable | Value | Description |
| --- | --- | --- | --- |
| `btn.primary.bg` | `--btn_primary_bg` | `#1a56db` | Retrofit-synthesized from styles/atlas.css:61 (declared as `--btn_primary_bg` in `:root`). |
| `btn.primary.bg.hover` | `--btn_primary_bg_hover` | `#1e429f` | Retrofit-synthesized from styles/atlas.css:62 (declared as `--btn_primary_bg_hover` in `:root`). |

### color (3)

| Token | CSS variable | Value | Description |
| --- | --- | --- | --- |
| `color.text.inverse` | `--color_text_inverse` | `#ffffff` | Retrofit-synthesized from styles/atlas.css:22 (declared as `--color_text_inverse` in `:root`). |
| `color.text.main` | `--color_text_main` | `#111827` | Retrofit-synthesized from styles/atlas.css:20 (declared as `--color_text_main` in `:root`). Re-declared in 1 scoped rule(s) (theme/media overrides) — base-scope value shown. |
| `color.text.subtle` | `--color_text_subtle` | `#6b7280` | Retrofit-synthesized from styles/atlas.css:21 (declared as `--color_text_subtle` in `:root`). |

### font (3)

| Token | CSS variable | Value | Description |
| --- | --- | --- | --- |
| `font.body` | `--font-body` | `"Helvetica Neue", Arial, sans-serif` | Retrofit-synthesized from styles/atlas.css:51 (declared as `--font-body` in `:root`). |
| `font.size.base` | `--fontSizeBase` | `14px` | Retrofit-synthesized from styles/atlas.css:52 (declared as `--fontSizeBase` in `:root`). |
| `font.size.small` | `--fontSizeSmall` | `12px` | Retrofit-synthesized from styles/atlas.css:53 (declared as `--fontSizeSmall` in `:root`). |

### fw (1)

| Token | CSS variable | Value | Description |
| --- | --- | --- | --- |
| `fw.bold` | `--fw-bold` | `600` | Retrofit-synthesized from styles/atlas.css:54 (declared as `--fw-bold` in `:root`). |

### input (1)

| Token | CSS variable | Value | Description |
| --- | --- | --- | --- |
| `input.border` | `--input-border` | `#d1d5db` | Retrofit-synthesized from styles/atlas.css:63 (declared as `--input-border` in `:root`). |

### link (1)

| Token | CSS variable | Value | Description |
| --- | --- | --- | --- |
| `link.color` | `--link-color` | `#1a56db` | Retrofit-synthesized from styles/atlas.css:36 (declared as `--link-color` in `:root`). |

### radius (3)

| Token | CSS variable | Value | Description |
| --- | --- | --- | --- |
| `radius.md` | `--radius_md` | `6px` | Retrofit-synthesized from styles/atlas.css:47 (declared as `--radius_md` in `:root`). |
| `radius.pill` | `--radius_pill` | `999px` | Retrofit-synthesized from styles/atlas.css:48 (declared as `--radius_pill` in `:root`). |
| `radius.sm` | `--radius_sm` | `3px` | Retrofit-synthesized from styles/atlas.css:46 (declared as `--radius_sm` in `:root`). |

### shadow (1)

| Token | CSS variable | Value | Description |
| --- | --- | --- | --- |
| `shadow.card` | `--shadow-card` | `0 1px 3px rgba(17, 24, 39, 0.12)` | Retrofit-synthesized from styles/atlas.css:57 (declared as `--shadow-card` in `:root`). |

### sp (5)

| Token | CSS variable | Value | Description |
| --- | --- | --- | --- |
| `sp.1` | `--sp-1` | `4px` | Retrofit-synthesized from styles/atlas.css:39 (declared as `--sp-1` in `:root`). |
| `sp.2` | `--sp-2` | `8px` | Retrofit-synthesized from styles/atlas.css:40 (declared as `--sp-2` in `:root`). |
| `sp.3` | `--sp-3` | `12px` | Retrofit-synthesized from styles/atlas.css:41 (declared as `--sp-3` in `:root`). |
| `sp.4` | `--sp-4` | `16px` | Retrofit-synthesized from styles/atlas.css:42 (declared as `--sp-4` in `:root`). |
| `sp.6` | `--sp-6` | `24px` | Retrofit-synthesized from styles/atlas.css:43 (declared as `--sp-6` in `:root`). |

### surface (3)

| Token | CSS variable | Value | Description |
| --- | --- | --- | --- |
| `surface.card` | `--surface-card` | `#ffffff` | Retrofit-synthesized from styles/atlas.css:27 (declared as `--surface-card` in `:root`). |
| `surface.default` | `--surface-default` | `#ffffff` | Retrofit-synthesized from styles/atlas.css:25 (declared as `--surface-default` in `:root`). Re-declared in 1 scoped rule(s) (theme/media overrides) — base-scope value shown. |
| `surface.sunken` | `--surface-sunken` | `#f3f4f6` | Retrofit-synthesized from styles/atlas.css:26 (declared as `--surface-sunken` in `:root`). |

### transition (1)

| Token | CSS variable | Value | Description |
| --- | --- | --- | --- |
| `transition.fast` | `--transition-fast` | `150ms` | Retrofit-synthesized from styles/atlas.css:58 (declared as `--transition-fast` in `:root`). |

### tw (7)

| Token | CSS variable | Value | Description |
| --- | --- | --- | --- |
| `tw.color.accent` | `--tw-color-accent` | `#7c3aed` | Retrofit-synthesized from tailwind.config.js:16 (theme.extend.colors.accent). PROPOSED custom property — adopt `--tw-color-accent` in shipped CSS to activate it. |
| `tw.color.brand` | `--tw-color-brand` | `#1a56db` | Retrofit-synthesized from tailwind.config.js:12 (theme.extend.colors.brand.DEFAULT). PROPOSED custom property — adopt `--tw-color-brand` in shipped CSS to activate it. |
| `tw.color.brand.600` | `--tw-color-brand-600` | `#1a56db` | Retrofit-synthesized from tailwind.config.js:13 (theme.extend.colors.brand.600). PROPOSED custom property — adopt `--tw-color-brand-600` in shipped CSS to activate it. |
| `tw.color.brand.700` | `--tw-color-brand-700` | `#1e429f` | Retrofit-synthesized from tailwind.config.js:14 (theme.extend.colors.brand.700). PROPOSED custom property — adopt `--tw-color-brand-700` in shipped CSS to activate it. |
| `tw.color.danger` | `--tw-color-danger` | `#b91c1c` | Retrofit-synthesized from tailwind.config.js:17 (theme.extend.colors.danger). PROPOSED custom property — adopt `--tw-color-danger` in shipped CSS to activate it. |
| `tw.radius.card` | `--tw-radius-card` | `6px` | Retrofit-synthesized from tailwind.config.js:23 (theme.extend.borderRadius.card). PROPOSED custom property — adopt `--tw-radius-card` in shipped CSS to activate it. |
| `tw.spacing.18` | `--tw-spacing-18` | `4.5rem` | Retrofit-synthesized from tailwind.config.js:20 (theme.extend.spacing.18). PROPOSED custom property — adopt `--tw-spacing-18` in shipped CSS to activate it. |

## Component tier (per-component theming hooks)
