{
  "$description": "GENERATED closed-world component registry for @ds/react. Any component not listed here does not exist. Each entry's `props` lists the component's OWN props; when `racBase` is set, the props of that react-aria-components base component are ALSO legal on the component (own props overriding/narrowing them), and `racProps` lists the commonly needed inherited subset — controlled usage (value/onChange, isSelected, selectedKey/onSelectionChange), isDisabled/isInvalid/isRequired, form `name`, placeholder — with exact extracted types (see `racPropsNote`; a prop the component omits from its base never appears). `tokenPrefix` names the component-tier `--ds-<tokenPrefix>-*` token namespace that is the component's customer override surface; components sharing a prefix form one theming family (e.g. Button and IconButton share `button`) and NEVER consume another family's hooks (NR-008); null means the component has no component-tier hooks. `storyFile` is the repo-relative path to the component's Storybook stories — the ground-truth usage examples; sub-components point at their parent's stories. Regenerate with: pnpm --filter @ds/react generate",
  "package": "@ds/react",
  "components": [
    {
      "name": "Alert",
      "description": "Static status message rendered as a plain `<div>` with a tone-mapped ARIA\nrole: interruptive tones (`danger`, `warning`) use `role=\"alert\"` so\nassistive technology announces them immediately; calm tones (`info`,\n`success`) use the polite `role=\"status\"`. For permanent, static content\n(a danger-zone explainer, a standing notice) pass `isLive={false}` to keep\nthe tone styling while dropping the live-region role entirely, so the\nmessage is not re-announced on every page load. Tones follow the soft\nrecipe — status surface background, status border, status text — through\nthe `--ds-alert-*` component hooks, with a decorative tone icon. An\noptional dismiss control (react-aria-components `Button` internally)\nappears when `onDismiss` is provided; the caller owns removing the alert.",
      "racBase": null,
      "racPropsNote": null,
      "racProps": null,
      "tokenPrefix": "alert",
      "storyFile": "packages/react/src/components/Alert/Alert.stories.tsx",
      "example": "<Alert>Your changes were saved as a draft.</Alert>\n<Alert tone=\"success\" title=\"Deployed\">Build 42 is live.</Alert>\n<Alert tone=\"danger\" onDismiss={() => setVisible(false)}>\n  Payment failed — check your card details.\n</Alert>\n<Alert tone=\"danger\" isLive={false} title=\"Danger zone\">\n  Deleting the workspace permanently removes all projects.\n</Alert>",
      "props": [
        {
          "name": "className",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Additional CSS class appended after the component's own classes.\nPlain string only; inline `style` is intentionally not supported\n(token rule)."
        },
        {
          "name": "isLive",
          "type": "boolean",
          "required": false,
          "defaultValue": "true",
          "description": "Whether the alert is a live region that assistive technology announces\nwhen it appears. When `true` (default), the tone decides the ARIA role\n(`alert` for `danger`/`warning`, `status` for `info`/`success`). Set to\n`false` for permanent, static content — e.g. a danger-zone explainer\nthat is part of the page rather than a notification — so the message is\nNOT re-announced on every load: the alert then renders as a plain\n`<div>` with the tone styling and no live-region role at all."
        },
        {
          "name": "onDismiss",
          "type": "() => void",
          "required": false,
          "defaultValue": null,
          "description": "When provided, the alert renders a dismiss control (an icon-only\naccessible button labeled \"Dismiss\") and calls this handler when it is\npressed. The alert does not remove itself — the caller owns visibility."
        },
        {
          "name": "title",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Optional bold heading line rendered above the message. Replaces the\nnative HTML tooltip attribute of the same name — it is rendered as\nvisible text, never as a tooltip."
        },
        {
          "name": "tone",
          "type": "\"info\" | \"success\" | \"warning\" | \"danger\"",
          "required": false,
          "defaultValue": "'info'",
          "description": "Status intent of the alert. Soft recipe: each tone pairs a status\nsurface background with its matching status border and text colors —\nnever a solid fill. While the alert is live (see `isLive`), the tone\nalso decides the ARIA role: `danger` and `warning` are interruptive and\nrender `role=\"alert\"`; `info` and `success` render the polite\n`role=\"status\"`."
        }
      ]
    },
    {
      "name": "Badge",
      "description": "Static status label rendered as a plain `<span>`. Purely presentational:\nno interaction, no focus, no ARIA role beyond the text itself. Tones use\nthe soft recipe — a `*-surface` background with the matching `*-text`\nforeground via the `--ds-badge-*` component hooks.",
      "racBase": null,
      "racPropsNote": null,
      "racProps": null,
      "tokenPrefix": "badge",
      "storyFile": "packages/react/src/components/Badge/Badge.stories.tsx",
      "example": "<Badge>Draft</Badge>\n<Badge tone=\"success\">Active</Badge>\n<Badge tone=\"danger\">Failed</Badge>",
      "props": [
        {
          "name": "className",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Additional CSS class appended after the component's own classes.\nPlain string only; inline `style` is intentionally not supported\n(token rule)."
        },
        {
          "name": "tone",
          "type": "\"info\" | \"success\" | \"warning\" | \"danger\" | \"neutral\"",
          "required": false,
          "defaultValue": "'neutral'",
          "description": "Status intent of the badge. `neutral` is a plain label, `info` an\ninformational note, `success` a positive state, `warning` a caution,\n`danger` an error or destructive state. Soft recipe: each tone pairs a\nstatus surface background with its matching status text color — never a\nsolid fill."
        }
      ]
    },
    {
      "name": "Button",
      "description": "Accessible push button built on react-aria-components `Button`.\nKeyboard behavior, focus management, and press semantics come from React\nAria; visual states are driven purely by RAC data attributes\n(`data-hovered`, `data-pressed`, `data-focus-visible`, `data-disabled`,\n`data-pending`) and `--ds-*` tokens.",
      "racBase": "Button",
      "racPropsNote": "Commonly needed props inherited from the react-aria-components base; the full react-aria-components ButtonProps surface is also legal.",
      "racProps": [
        {
          "name": "autoFocus",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether the element should receive focus on render."
        },
        {
          "name": "isDisabled",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether the button is disabled."
        },
        {
          "name": "name",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Submitted as a pair with the button's value as part of the form data."
        },
        {
          "name": "onPress",
          "type": "(e: PressEvent) => void",
          "required": false,
          "defaultValue": null,
          "description": "Handler that is called when the press is released over the target."
        },
        {
          "name": "value",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "The value associated with the button's name when it's submitted with the form data."
        }
      ],
      "tokenPrefix": "button",
      "storyFile": "packages/react/src/components/Button/Button.stories.tsx",
      "example": "<Button onPress={save}>Save</Button>\n<Button variant=\"secondary\" size=\"lg\" onPress={cancel}>Cancel</Button>\n<Button variant=\"danger\" isLoading>Deleting…</Button>",
      "props": [
        {
          "name": "className",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Additional CSS class appended after the component's own classes.\nNarrowed from the react-aria-components render-prop form to a plain\nstring; inline `style` is intentionally not supported (token rule)."
        },
        {
          "name": "isLoading",
          "type": "boolean",
          "required": false,
          "defaultValue": "false",
          "description": "Marks the button as busy with an in-progress action: interaction is\ndisabled, the state is announced to assistive technology, and a\nCSS-only spinner is shown before the label. The label stays visible."
        },
        {
          "name": "size",
          "type": "\"sm\" | \"md\" | \"lg\"",
          "required": false,
          "defaultValue": "'md'",
          "description": "Control size. Maps to the `--ds-size-control-*` height scale."
        },
        {
          "name": "variant",
          "type": "\"danger\" | \"primary\" | \"secondary\" | \"ghost\"",
          "required": false,
          "defaultValue": "'primary'",
          "description": "Visual intent of the button. `primary` is the single main action of a\nview, `secondary` a supporting action, `ghost` a low-emphasis inline\naction, `danger` a destructive action."
        }
      ]
    },
    {
      "name": "Card",
      "description": "Static, purely presentational surface container (a plain `<div>`, no\nrole). Groups related content on a token-driven card surface. The ONLY\nsanctioned composition slots are `CardHeader`, `CardBody`, and\n`CardFooter` — there are no other slot components; layout inside a slot\nis plain HTML with space tokens.",
      "racBase": null,
      "racPropsNote": null,
      "racProps": null,
      "tokenPrefix": "card",
      "storyFile": "packages/react/src/components/Card/Card.stories.tsx",
      "example": "<Card>\n  <CardBody>Body-only card</CardBody>\n</Card>\n<Card elevation=\"raised\">\n  <CardHeader>\n    <h3>Plan</h3>\n  </CardHeader>\n  <CardBody>Everything in Starter, plus unlimited seats.</CardBody>\n  <CardFooter>\n    <Button onPress={upgrade}>Upgrade</Button>\n  </CardFooter>\n</Card>",
      "props": [
        {
          "name": "className",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Additional CSS class appended after the component's own classes.\nInline `style` is intentionally not supported (token rule)."
        },
        {
          "name": "elevation",
          "type": "\"flat\" | \"raised\"",
          "required": false,
          "defaultValue": "'flat'",
          "description": "Elevation treatment of the card surface. `flat` outlines the card with\na border; `raised` lifts it with the card shadow (plus a hairline\nborder under the shadow)."
        }
      ]
    },
    {
      "name": "CardBody",
      "description": "Card slot: main content region of a `Card`. Static presentational\n`<div>` carrying the card's internal padding.",
      "racBase": null,
      "racPropsNote": null,
      "racProps": null,
      "tokenPrefix": "card",
      "storyFile": "packages/react/src/components/Card/Card.stories.tsx",
      "example": "<Card>\n  <CardBody>Usage this month: 4,210 requests.</CardBody>\n</Card>",
      "props": [
        {
          "name": "className",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Additional CSS class appended after the component's own classes.\nInline `style` is intentionally not supported (token rule)."
        }
      ]
    },
    {
      "name": "CardFooter",
      "description": "Card slot: trailing region of a `Card` (typically actions), separated\nfrom the content above by a hairline border. Static presentational\n`<div>`.",
      "racBase": null,
      "racPropsNote": null,
      "racProps": null,
      "tokenPrefix": "card",
      "storyFile": "packages/react/src/components/Card/Card.stories.tsx",
      "example": "<Card>\n  <CardBody>Delete this project?</CardBody>\n  <CardFooter>\n    <Button variant=\"danger\" onPress={confirm}>Delete</Button>\n  </CardFooter>\n</Card>",
      "props": [
        {
          "name": "className",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Additional CSS class appended after the component's own classes.\nInline `style` is intentionally not supported (token rule)."
        }
      ]
    },
    {
      "name": "CardHeader",
      "description": "Card slot: leading region of a `Card`, separated from the content below\nby a hairline border. Static presentational `<div>`; put your own\nsemantic heading element inside.",
      "racBase": null,
      "racPropsNote": null,
      "racProps": null,
      "tokenPrefix": "card",
      "storyFile": "packages/react/src/components/Card/Card.stories.tsx",
      "example": "<Card>\n  <CardHeader>\n    <h3>Team members</h3>\n  </CardHeader>\n  <CardBody>…</CardBody>\n</Card>",
      "props": [
        {
          "name": "className",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Additional CSS class appended after the component's own classes.\nInline `style` is intentionally not supported (token rule)."
        }
      ]
    },
    {
      "name": "Checkbox",
      "description": "Accessible checkbox built on react-aria-components `Checkbox`. The visible\nbox is CSS-drawn over a visually hidden native input; the check and\nindeterminate marks are inline SVG stroked with `currentColor`. Keyboard\nbehavior (Space toggles), focus management, and ARIA state come from React\nAria; visual states are driven purely by RAC data attributes\n(`data-selected`, `data-indeterminate`, `data-hovered`, `data-pressed`,\n`data-focus-visible`, `data-disabled`, `data-invalid`) and `--ds-*` tokens.",
      "racBase": "Checkbox",
      "racPropsNote": "Commonly needed props inherited from the react-aria-components base; the full react-aria-components CheckboxProps surface is also legal.",
      "racProps": [
        {
          "name": "autoFocus",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether the element should receive focus on render."
        },
        {
          "name": "isReadOnly",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether the input can be selected but not changed by the user."
        },
        {
          "name": "name",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "The name of the input element, used when submitting an HTML form. See\n[MDN](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input#htmlattrdefname)."
        },
        {
          "name": "onPress",
          "type": "(e: PressEvent) => void",
          "required": false,
          "defaultValue": null,
          "description": "Handler that is called when the press is released over the target."
        },
        {
          "name": "value",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "The value of the input element, used when submitting an HTML form. See\n[MDN](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input#htmlattrdefvalue)."
        }
      ],
      "tokenPrefix": "checkbox",
      "storyFile": "packages/react/src/components/Checkbox/Checkbox.stories.tsx",
      "example": "<Checkbox onChange={setAccepted}>Accept terms</Checkbox>\n<Checkbox isSelected={allSelected} isIndeterminate={someSelected} onChange={toggleAll}>\n  Select all\n</Checkbox>\n<Checkbox isRequired isInvalid={showError}>I agree to the policy</Checkbox>",
      "props": [
        {
          "name": "children",
          "type": "ReactNode",
          "required": false,
          "defaultValue": null,
          "description": "Label content rendered next to the box. Always provide a visible label;\nthe whole label is the click target."
        },
        {
          "name": "className",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Additional CSS class appended after the component's own classes.\nNarrowed from the react-aria-components render-prop form to a plain\nstring; inline `style` is intentionally not supported (token rule)."
        },
        {
          "name": "defaultSelected",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether the checkbox is selected initially (uncontrolled)."
        },
        {
          "name": "isDisabled",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Disables the checkbox: interaction is blocked and the native input is\ndisabled."
        },
        {
          "name": "isIndeterminate",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Marks the checkbox as indeterminate (\"mixed\"): a dash is shown instead\nof a check and assistive technology announces the mixed state. Typical\nfor a \"select all\" control over a partially selected set."
        },
        {
          "name": "isInvalid",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Marks the checkbox as failing validation; the box takes the danger\ntreatment and the state is exposed to assistive technology."
        },
        {
          "name": "isRequired",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Marks the checkbox as required before form submission."
        },
        {
          "name": "isSelected",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether the checkbox is selected (controlled). Pair with `onChange`."
        },
        {
          "name": "onChange",
          "type": "(isSelected: boolean) => void",
          "required": false,
          "defaultValue": null,
          "description": "Handler called when the selection state changes. Receives the next\nselected state."
        }
      ]
    },
    {
      "name": "Dialog",
      "description": "Modal dialog built on react-aria-components `DialogTrigger` + `Modal` +\n`Dialog`. Focus trapping, scroll locking, Escape/backdrop dismissal, and\n`aria-labelledby` wiring all come from React Aria; entry/exit states are\ndriven by RAC data attributes (`data-entering`, `data-exiting`) with\n`--ds-motion-*` tokens, gated by `prefers-reduced-motion`.",
      "racBase": "Dialog",
      "racPropsNote": "Commonly needed props inherited from the react-aria-components base; the full react-aria-components DialogProps surface is also legal.",
      "racProps": [],
      "tokenPrefix": "dialog",
      "storyFile": "packages/react/src/components/Dialog/Dialog.stories.tsx",
      "example": "<Dialog title=\"Delete project\" trigger={<Button variant=\"danger\">Delete…</Button>}>\n  {({ close }) => (\n    <>\n      <p>This action cannot be undone.</p>\n      <Button variant=\"danger\" onPress={close}>Confirm delete</Button>\n    </>\n  )}\n</Dialog>\n\n<Dialog title=\"Settings\" size=\"lg\" isOpen={isOpen} onOpenChange={setOpen}>\n  <SettingsForm />\n</Dialog>",
      "props": [
        {
          "name": "className",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Additional CSS class appended after the component's own classes on the\ndialog panel. Narrowed to a plain string; inline `style` is intentionally\nnot supported (token rule)."
        },
        {
          "name": "defaultOpen",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether the dialog is initially open (uncontrolled)."
        },
        {
          "name": "isDismissable",
          "type": "boolean",
          "required": false,
          "defaultValue": "true",
          "description": "Whether the dialog can be dismissed by clicking the backdrop or pressing\nEscape. Set to `false` for flows that require an explicit choice; provide\na closing action via the `children` render function's `close` callback."
        },
        {
          "name": "isOpen",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether the dialog is open (controlled)."
        },
        {
          "name": "onOpenChange",
          "type": "(isOpen: boolean) => void",
          "required": false,
          "defaultValue": null,
          "description": "Handler called when the dialog's open state changes (trigger pressed,\nEscape, backdrop click, or the `close` render-prop callback)."
        },
        {
          "name": "size",
          "type": "\"sm\" | \"md\" | \"lg\"",
          "required": false,
          "defaultValue": "'md'",
          "description": "Width step of the dialog panel, applied as a `max-inline-size` cap; the\npanel shrinks to fit small viewports."
        },
        {
          "name": "title",
          "type": "string",
          "required": true,
          "defaultValue": null,
          "description": "Dialog title. Rendered as the react-aria-components `Heading` with\n`slot=\"title\"`, which wires the dialog's `aria-labelledby` to it\nautomatically."
        },
        {
          "name": "trigger",
          "type": "ReactElement<unknown, string | JSXElementConstructor<any>>",
          "required": false,
          "defaultValue": null,
          "description": "Optional trigger element (e.g. a `Button`). When provided, the dialog is\nwrapped in a react-aria-components `DialogTrigger` and opens when the\ntrigger is pressed. Omit it to control the dialog yourself via `isOpen`."
        }
      ]
    },
    {
      "name": "IconButton",
      "description": "Icon-only push button built on react-aria-components `Button`. A square\ncontrol (width and height both from the button height scale) whose single\nchild is a decorative icon; `aria-label` is required at the type level\nbecause it is the control's only accessible name. Keyboard behavior, focus\nmanagement, and press semantics come from React Aria; visual states are\ndriven purely by RAC data attributes (`data-hovered`, `data-pressed`,\n`data-focus-visible`, `data-disabled`) and `--ds-*` tokens.\n\nDocumented NR-008 exception (lead decision): IconButton belongs to the\nbutton family and consumes the `--ds-button-*` component-tier hook\nnamespace rather than forking an `--ds-iconbutton-*` namespace of its own,\nso customer overrides of the button family restyle both controls together.",
      "racBase": "Button",
      "racPropsNote": "Commonly needed props inherited from the react-aria-components base; the full react-aria-components ButtonProps surface is also legal.",
      "racProps": [
        {
          "name": "autoFocus",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether the element should receive focus on render."
        },
        {
          "name": "isDisabled",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether the button is disabled."
        },
        {
          "name": "name",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Submitted as a pair with the button's value as part of the form data."
        },
        {
          "name": "onPress",
          "type": "(e: PressEvent) => void",
          "required": false,
          "defaultValue": null,
          "description": "Handler that is called when the press is released over the target."
        },
        {
          "name": "value",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "The value associated with the button's name when it's submitted with the form data."
        }
      ],
      "tokenPrefix": "button",
      "storyFile": "packages/react/src/components/IconButton/IconButton.stories.tsx",
      "example": "<IconButton aria-label=\"Close\" onPress={close}>\n  <CloseIcon />\n</IconButton>\n<IconButton aria-label=\"Add item\" variant=\"primary\" size=\"lg\" onPress={add}>\n  <PlusIcon />\n</IconButton>\n<IconButton aria-label=\"Delete row\" variant=\"danger\" onPress={remove}>\n  <TrashIcon />\n</IconButton>",
      "props": [
        {
          "name": "aria-label",
          "type": "string",
          "required": true,
          "defaultValue": null,
          "description": "Accessible name for the button. Required at the type level: the icon is\ndecorative (rendered `aria-hidden`), so this label is the ONLY accessible\nname the control has. There is no escape hatch."
        },
        {
          "name": "children",
          "type": "ReactNode",
          "required": false,
          "defaultValue": null,
          "description": "The icon to render. Presented to assistive technology as decorative\n(`aria-hidden`); the accessible name comes from `aria-label`."
        },
        {
          "name": "className",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Additional CSS class appended after the component's own classes.\nNarrowed from the react-aria-components render-prop form to a plain\nstring; inline `style` is intentionally not supported (token rule)."
        },
        {
          "name": "size",
          "type": "\"sm\" | \"md\" | \"lg\"",
          "required": false,
          "defaultValue": "'md'",
          "description": "Control size. The button is square: both dimensions come from the\nbutton height scale."
        },
        {
          "name": "variant",
          "type": "\"danger\" | \"primary\" | \"secondary\" | \"ghost\"",
          "required": false,
          "defaultValue": "'ghost'",
          "description": "Visual intent of the button. `ghost` is the low-emphasis default for\ntoolbar/inline icon actions; `primary` is a main action, `secondary` a\nsupporting action, `danger` a destructive action."
        }
      ]
    },
    {
      "name": "Radio",
      "description": "A single option inside a `RadioGroup`, built on react-aria-components\n`Radio`. The circular control is drawn entirely in CSS (no icon assets)\nand reflects selection, hover, focus, invalid, and disabled states via\nRAC data attributes (`data-selected`, `data-hovered`,\n`data-focus-visible`, `data-invalid`, `data-disabled`).",
      "racBase": "Radio",
      "racPropsNote": "Commonly needed props inherited from the react-aria-components base; the full react-aria-components RadioProps surface is also legal.",
      "racProps": [
        {
          "name": "autoFocus",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether the element should receive focus on render."
        },
        {
          "name": "isDisabled",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether the radio button is disabled or not.\nShows that a selection exists, but is not available in that circumstance."
        },
        {
          "name": "onPress",
          "type": "(e: PressEvent) => void",
          "required": false,
          "defaultValue": null,
          "description": "Handler that is called when the press is released over the target."
        }
      ],
      "tokenPrefix": "radio",
      "storyFile": "packages/react/src/components/RadioGroup/RadioGroup.stories.tsx",
      "example": "<Radio value=\"email\">Email</Radio>\n<Radio value=\"sms\" isDisabled>SMS</Radio>",
      "props": [
        {
          "name": "children",
          "type": "ReactNode",
          "required": true,
          "defaultValue": null,
          "description": "Label content rendered next to the CSS-drawn control. Required: every\nradio needs a visible accessible name."
        },
        {
          "name": "className",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Additional CSS class appended after the component's own classes.\nNarrowed from the react-aria-components render-prop form to a plain\nstring; inline `style` is intentionally not supported (token rule)."
        },
        {
          "name": "value",
          "type": "string",
          "required": true,
          "defaultValue": null,
          "description": "Value this option contributes to the group: it is compared against the\ngroup's `value`/`defaultValue` and passed to the group's `onChange`\nwhen the option is selected."
        }
      ]
    },
    {
      "name": "RadioGroup",
      "description": "A labeled group of mutually exclusive options built on\nreact-aria-components `RadioGroup`. Roving tabindex, arrow-key navigation,\nand group labelling come from React Aria; visual states are driven purely\nby RAC data attributes (`data-disabled`, `data-invalid`,\n`data-orientation`) and `--ds-*` tokens. Selection is single-value:\n`value`/`defaultValue` plus `onChange` receive the selected `Radio`'s\n`value` string.",
      "racBase": "RadioGroup",
      "racPropsNote": "Commonly needed props inherited from the react-aria-components base; the full react-aria-components RadioGroupProps surface is also legal.",
      "racProps": [
        {
          "name": "defaultValue",
          "type": "string | null",
          "required": false,
          "defaultValue": null,
          "description": "The default value (uncontrolled)."
        },
        {
          "name": "isDisabled",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether the input is disabled."
        },
        {
          "name": "isInvalid",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether the input value is invalid."
        },
        {
          "name": "isReadOnly",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether the input can be selected but not changed by the user."
        },
        {
          "name": "isRequired",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether user input is required on the input before form submission."
        },
        {
          "name": "name",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "The name of the input element, used when submitting an HTML form. See\n[MDN](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input#htmlattrdefname)."
        },
        {
          "name": "onChange",
          "type": "(value: string) => void",
          "required": false,
          "defaultValue": null,
          "description": "Handler that is called when the value changes."
        },
        {
          "name": "value",
          "type": "string | null",
          "required": false,
          "defaultValue": null,
          "description": "The current value (controlled)."
        }
      ],
      "tokenPrefix": "radio",
      "storyFile": "packages/react/src/components/RadioGroup/RadioGroup.stories.tsx",
      "example": "<RadioGroup label=\"Notification method\" onChange={setMethod}>\n  <Radio value=\"email\">Email</Radio>\n  <Radio value=\"sms\">SMS</Radio>\n</RadioGroup>\n<RadioGroup label=\"Density\" orientation=\"horizontal\" defaultValue=\"cozy\">\n  <Radio value=\"compact\">Compact</Radio>\n  <Radio value=\"cozy\">Cozy</Radio>\n</RadioGroup>\n<RadioGroup label=\"Plan\" isInvalid errorMessage=\"Choose a plan to continue.\">\n  <Radio value=\"free\">Free</Radio>\n  <Radio value=\"pro\">Pro</Radio>\n</RadioGroup>",
      "props": [
        {
          "name": "children",
          "type": "ReactNode",
          "required": true,
          "defaultValue": null,
          "description": "The `Radio` options of the group."
        },
        {
          "name": "className",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Additional CSS class appended after the component's own classes.\nNarrowed from the react-aria-components render-prop form to a plain\nstring; inline `style` is intentionally not supported (token rule)."
        },
        {
          "name": "description",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Optional helper text rendered below the label and associated with the\ngroup via `aria-describedby`."
        },
        {
          "name": "errorMessage",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Error text rendered below the options. Only shown while the group is\ninvalid (`isInvalid` or form validation); it is associated with the\ngroup via `aria-describedby`."
        },
        {
          "name": "label",
          "type": "string",
          "required": true,
          "defaultValue": null,
          "description": "Visible label naming the group. Required: a radio group without an\naccessible name is an accessibility failure, so the label is part of the\ntyped contract rather than an optional slot."
        },
        {
          "name": "orientation",
          "type": "\"horizontal\" | \"vertical\"",
          "required": false,
          "defaultValue": "'vertical'",
          "description": "Layout axis of the options. `vertical` stacks the radios, `horizontal`\nlays them out in a wrapping row. Keyboard arrow navigation follows the\norientation automatically via React Aria."
        }
      ]
    },
    {
      "name": "Select",
      "description": "Accessible single-select built on react-aria-components `Select`,\ncomposing the RAC `Label`, `Button`, `SelectValue`, `Popover`, and\n`ListBox` parts internally behind a data-driven `items` API. Keyboard\nbehavior (arrow keys, typeahead, Enter/Escape), focus management, and\nlabel/description/error wiring come from React Aria; visual states are\ndriven purely by RAC data attributes (`data-open`, `data-invalid`,\n`data-disabled`, `data-focused`, `data-selected`) and `--ds-*` tokens.\n\nThe trigger consumes the SHARED `--ds-field-*` token namespace — one\noverride surface intentionally shared by TextField, TextArea, and Select\nso all form fields re-theme together.",
      "racBase": "Select",
      "racPropsNote": "Commonly needed props inherited from the react-aria-components base; the full react-aria-components SelectProps surface is also legal.",
      "racProps": [
        {
          "name": "autoFocus",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether the element should receive focus on render."
        },
        {
          "name": "defaultOpen",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Sets the default open state of the menu."
        },
        {
          "name": "isOpen",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Sets the open state of the menu."
        },
        {
          "name": "name",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "The name of the input, used when submitting an HTML form."
        },
        {
          "name": "onOpenChange",
          "type": "(isOpen: boolean) => void",
          "required": false,
          "defaultValue": null,
          "description": "Method that is called when the open state of the menu changes."
        }
      ],
      "tokenPrefix": "field",
      "storyFile": "packages/react/src/components/Select/Select.stories.tsx",
      "example": "<Select\n  label=\"Favorite fruit\"\n  placeholder=\"Pick a fruit\"\n  items={[\n    { id: 'apple', label: 'Apple' },\n    { id: 'banana', label: 'Banana' },\n  ]}\n  onSelectionChange={setFruit}\n/>\n<Select label=\"Plan\" items={plans} selectedKey={plan} onSelectionChange={setPlan} isRequired />\n<Select label=\"Region\" items={regions} isInvalid errorMessage=\"Pick a region.\" />",
      "props": [
        {
          "name": "className",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Additional CSS class appended after the component's own classes.\nNarrowed from the react-aria-components render-prop form to a plain\nstring; inline `style` is intentionally not supported (token rule)."
        },
        {
          "name": "defaultSelectedKey",
          "type": "string | number | null",
          "required": false,
          "defaultValue": null,
          "description": "The `id` of the initially selected option (uncontrolled)."
        },
        {
          "name": "description",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Help text rendered below the field and linked to the trigger via\n`aria-describedby`."
        },
        {
          "name": "errorMessage",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Error text rendered when the field is invalid (via `isInvalid` or\nfailed native validation) and linked via `aria-describedby`. Hidden\nwhile the field is valid."
        },
        {
          "name": "isDisabled",
          "type": "boolean",
          "required": false,
          "defaultValue": "false",
          "description": "Whether the field is disabled: the trigger cannot be focused or opened."
        },
        {
          "name": "isInvalid",
          "type": "boolean",
          "required": false,
          "defaultValue": "false",
          "description": "Whether the field value is invalid. Drives the error border, the\n`data-invalid` state, and rendering of `errorMessage`."
        },
        {
          "name": "isRequired",
          "type": "boolean",
          "required": false,
          "defaultValue": "false",
          "description": "Whether a selection is required before form submission."
        },
        {
          "name": "items",
          "type": "{ id: string | number; label: string; isDisabled?: boolean; }[]",
          "required": true,
          "defaultValue": null,
          "description": "Typed options (v1 is a data-driven API, not free composition). Each\noption's `id` is the selection key and `label` is the visible and\naccessible text; `isDisabled` renders the option non-selectable."
        },
        {
          "name": "label",
          "type": "string",
          "required": true,
          "defaultValue": null,
          "description": "Visible field label. Required: the trigger's accessible name is derived\nfrom it, so a Select can never render unlabeled."
        },
        {
          "name": "onSelectionChange",
          "type": "(key: string | number | null) => void",
          "required": false,
          "defaultValue": null,
          "description": "Handler called with the selected option's `id` (or `null`) when the\nselection changes."
        },
        {
          "name": "placeholder",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Text shown in the trigger while no option is selected."
        },
        {
          "name": "selectedKey",
          "type": "string | number | null",
          "required": false,
          "defaultValue": null,
          "description": "The `id` of the currently selected option (controlled). `null` means no\nselection."
        }
      ]
    },
    {
      "name": "Switch",
      "description": "Accessible on/off switch built on react-aria-components `Switch`. The\nvisible track and thumb are CSS-drawn over a visually hidden native input\n(`role=\"switch\"`); the thumb slides with motion tokens and settles\ninstantly under `prefers-reduced-motion`. Keyboard behavior (Space\ntoggles), focus management, and ARIA state come from React Aria; visual\nstates are driven purely by RAC data attributes (`data-selected`,\n`data-hovered`, `data-pressed`, `data-focus-visible`, `data-disabled`)\nand `--ds-*` tokens.",
      "racBase": "Switch",
      "racPropsNote": "Commonly needed props inherited from the react-aria-components base; the full react-aria-components SwitchProps surface is also legal.",
      "racProps": [
        {
          "name": "autoFocus",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether the element should receive focus on render."
        },
        {
          "name": "isReadOnly",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether the input can be selected but not changed by the user."
        },
        {
          "name": "name",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "The name of the input element, used when submitting an HTML form. See\n[MDN](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input#htmlattrdefname)."
        },
        {
          "name": "onPress",
          "type": "(e: PressEvent) => void",
          "required": false,
          "defaultValue": null,
          "description": "Handler that is called when the press is released over the target."
        },
        {
          "name": "value",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "The value of the input element, used when submitting an HTML form. See\n[MDN](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input#htmlattrdefvalue)."
        }
      ],
      "tokenPrefix": "switch",
      "storyFile": "packages/react/src/components/Switch/Switch.stories.tsx",
      "example": "<Switch onChange={setNotifications}>Enable notifications</Switch>\n<Switch isSelected={isDark} onChange={setIsDark}>Dark mode</Switch>\n<Switch isDisabled>Beta features</Switch>",
      "props": [
        {
          "name": "children",
          "type": "ReactNode",
          "required": false,
          "defaultValue": null,
          "description": "Label content rendered next to the track. Always provide a visible\nlabel; the whole label is the click target."
        },
        {
          "name": "className",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Additional CSS class appended after the component's own classes.\nNarrowed from the react-aria-components render-prop form to a plain\nstring; inline `style` is intentionally not supported (token rule)."
        },
        {
          "name": "defaultSelected",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether the switch is on initially (uncontrolled)."
        },
        {
          "name": "isDisabled",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Disables the switch: interaction is blocked and the native input is\ndisabled."
        },
        {
          "name": "isSelected",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether the switch is on (controlled). Pair with `onChange`."
        },
        {
          "name": "onChange",
          "type": "(isSelected: boolean) => void",
          "required": false,
          "defaultValue": null,
          "description": "Handler called when the on/off state changes. Receives the next\nselected state."
        }
      ]
    },
    {
      "name": "Tab",
      "description": "A single tab trigger inside `TabList`, built on react-aria-components\n`Tab`. The `id` links it to the `TabPanel` with the same `id`. The\nselected tab shows an accent indicator line and emphasized text;\n`isDisabled` removes it from selection and keyboard navigation.",
      "racBase": "Tab",
      "racPropsNote": "Commonly needed props inherited from the react-aria-components base; the full react-aria-components TabProps surface is also legal.",
      "racProps": [
        {
          "name": "isDisabled",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether the tab is disabled."
        },
        {
          "name": "onPress",
          "type": "(e: PressEvent) => void",
          "required": false,
          "defaultValue": null,
          "description": "Handler that is called when the press is released over the target."
        }
      ],
      "tokenPrefix": "tabs",
      "storyFile": "packages/react/src/components/Tabs/Tabs.stories.tsx",
      "example": "<Tab id=\"billing\">Billing</Tab>\n<Tab id=\"danger\" isDisabled>Danger zone</Tab>",
      "props": [
        {
          "name": "className",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Additional CSS class appended after the component's own classes.\nNarrowed from the react-aria-components render-prop form to a plain\nstring; inline `style` is intentionally not supported (token rule)."
        }
      ]
    },
    {
      "name": "TabList",
      "description": "The row of tab triggers inside `Tabs`, built on react-aria-components\n`TabList`. Renders `role=\"tablist\"` and manages roving-tabindex focus for\nits `Tab` children. Give it an `aria-label` when there is no visible\nheading labelling the tab set.",
      "racBase": "TabList",
      "racPropsNote": "Commonly needed props inherited from the react-aria-components base; the full react-aria-components TabListProps surface is also legal.",
      "racProps": [],
      "tokenPrefix": "tabs",
      "storyFile": "packages/react/src/components/Tabs/Tabs.stories.tsx",
      "example": "<TabList aria-label=\"Account settings\">\n  <Tab id=\"profile\">Profile</Tab>\n  <Tab id=\"security\">Security</Tab>\n</TabList>",
      "props": [
        {
          "name": "className",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Additional CSS class appended after the component's own classes.\nNarrowed from the react-aria-components render-prop form to a plain\nstring; inline `style` is intentionally not supported (token rule)."
        }
      ]
    },
    {
      "name": "TabPanel",
      "description": "The content region for one tab, built on react-aria-components\n`TabPanel`. The `id` links it to the `Tab` with the same `id`; only the\nselected panel is mounted (pass `shouldForceMount` to keep it in the DOM).\nReact Aria labels the panel from its tab and makes it focusable when it\ncontains no focusable content.",
      "racBase": "TabPanel",
      "racPropsNote": "Commonly needed props inherited from the react-aria-components base; the full react-aria-components TabPanelProps surface is also legal.",
      "racProps": [],
      "tokenPrefix": "tabs",
      "storyFile": "packages/react/src/components/Tabs/Tabs.stories.tsx",
      "example": "<TabPanel id=\"billing\">\n  <p>Billing details…</p>\n</TabPanel>",
      "props": [
        {
          "name": "className",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Additional CSS class appended after the component's own classes.\nNarrowed from the react-aria-components render-prop form to a plain\nstring; inline `style` is intentionally not supported (token rule)."
        }
      ]
    },
    {
      "name": "Tabs",
      "description": "Container for a set of tabs, built on react-aria-components `Tabs`.\nCompose `TabList` (holding `Tab` triggers) with one `TabPanel` per tab;\nmatching `id`s wire each trigger to its panel. Selection state, keyboard\nnavigation (arrow keys, Home/End), and ARIA `tablist`/`tab`/`tabpanel`\nwiring all come from React Aria; visual states are driven purely by RAC\ndata attributes (`data-selected`, `data-hovered`, `data-focus-visible`,\n`data-disabled`) and `--ds-*` tokens. Horizontal only in v1.",
      "racBase": "Tabs",
      "racPropsNote": "Commonly needed props inherited from the react-aria-components base; the full react-aria-components TabsProps surface is also legal.",
      "racProps": [
        {
          "name": "defaultSelectedKey",
          "type": "Key",
          "required": false,
          "defaultValue": null,
          "description": "The initial selected keys in the collection (uncontrolled)."
        },
        {
          "name": "isDisabled",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether the TabList is disabled.\nShows that a selection exists, but is not available in that circumstance."
        },
        {
          "name": "onSelectionChange",
          "type": "(key: Key) => void",
          "required": false,
          "defaultValue": null,
          "description": "Handler that is called when the selection changes."
        },
        {
          "name": "selectedKey",
          "type": "Key",
          "required": false,
          "defaultValue": null,
          "description": "The currently selected key in the collection (controlled)."
        }
      ],
      "tokenPrefix": "tabs",
      "storyFile": "packages/react/src/components/Tabs/Tabs.stories.tsx",
      "example": "<Tabs defaultSelectedKey=\"overview\">\n  <TabList aria-label=\"Project sections\">\n    <Tab id=\"overview\">Overview</Tab>\n    <Tab id=\"settings\">Settings</Tab>\n  </TabList>\n  <TabPanel id=\"overview\">Overview content</TabPanel>\n  <TabPanel id=\"settings\">Settings content</TabPanel>\n</Tabs>",
      "props": [
        {
          "name": "className",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Additional CSS class appended after the component's own classes.\nNarrowed from the react-aria-components render-prop form to a plain\nstring; inline `style` is intentionally not supported (token rule)."
        }
      ]
    },
    {
      "name": "TextArea",
      "description": "Accessible multi-line text input built on react-aria-components\n`TextField` with a `TextArea` element. Label, description, and error\nwiring (`aria-describedby`, `aria-invalid`) come from React Aria; visual\nstates are driven purely by RAC data attributes (`data-hovered`,\n`data-focus-visible`, `data-disabled`, `data-invalid`) and `--ds-*`\ntokens. Styling consumes the SHARED `--ds-field-*` hook namespace — by\ndesign, TextField, TextArea, and Select theme together as one field\nsurface.",
      "racBase": "TextField",
      "racPropsNote": "Commonly needed props inherited from the react-aria-components base; the full react-aria-components TextFieldProps surface is also legal.",
      "racProps": [
        {
          "name": "autoFocus",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether the element should receive focus on render."
        },
        {
          "name": "defaultValue",
          "type": "string | null",
          "required": false,
          "defaultValue": null,
          "description": "The default value (uncontrolled)."
        },
        {
          "name": "isDisabled",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether the input is disabled."
        },
        {
          "name": "isInvalid",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether the value is invalid."
        },
        {
          "name": "isReadOnly",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether the input can be selected but not changed by the user."
        },
        {
          "name": "isRequired",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether user input is required on the input before form submission."
        },
        {
          "name": "name",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "The name of the input element, used when submitting an HTML form. See\n[MDN](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input#htmlattrdefname)."
        },
        {
          "name": "onChange",
          "type": "(value: string) => void",
          "required": false,
          "defaultValue": null,
          "description": "Handler that is called when the value changes."
        },
        {
          "name": "value",
          "type": "string | null",
          "required": false,
          "defaultValue": null,
          "description": "The current value (controlled)."
        }
      ],
      "tokenPrefix": "field",
      "storyFile": "packages/react/src/components/TextArea/TextArea.stories.tsx",
      "example": "<TextArea label=\"Message\" placeholder=\"Tell us more…\" />\n<TextArea label=\"Notes\" description=\"Visible to the whole team.\" rows={5} />\n<TextArea label=\"Feedback\" autoGrow isRequired isInvalid errorMessage=\"Feedback is required.\" />",
      "props": [
        {
          "name": "autoGrow",
          "type": "boolean",
          "required": false,
          "defaultValue": "false",
          "description": "Grow the field with its content instead of scrolling. Implemented with\nCSS `field-sizing: content` (no JS, no dependencies); browsers without\nsupport fall back to the fixed `rows` height with a scrollbar. Manual\nresize is disabled while auto-growing."
        },
        {
          "name": "className",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Additional CSS class appended after the component's own classes.\nNarrowed from the react-aria-components render-prop form to a plain\nstring; inline `style` is intentionally not supported (token rule)."
        },
        {
          "name": "description",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Optional help text rendered below the field and linked to the textarea\nvia `aria-describedby`."
        },
        {
          "name": "errorMessage",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Error text rendered when the field is invalid (via `isInvalid` or a\nfailed `validate`). When omitted, react-aria-components' built-in\nvalidation messages are shown instead."
        },
        {
          "name": "label",
          "type": "string",
          "required": true,
          "defaultValue": null,
          "description": "Visible label for the field. Always rendered and associated with the\ntextarea for assistive technology — there is no unlabeled variant."
        },
        {
          "name": "placeholder",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Temporary hint text shown while the field is empty. Never a substitute\nfor `label`."
        },
        {
          "name": "rows",
          "type": "number",
          "required": false,
          "defaultValue": "3",
          "description": "Initial visible text rows. With `autoGrow`, serves as the fallback\nheight in browsers without `field-sizing` support."
        }
      ]
    },
    {
      "name": "TextField",
      "description": "Accessible single-line text input built on react-aria-components\n`TextField`, composing RAC `Label`, `Input`, `Text` (description), and\n`FieldError` internally — label association, description/error wiring\n(`aria-describedby`), and invalid-state announcement come from React Aria.\nValidation and state flow through the RAC props (`isInvalid`,\n`isRequired`, `isDisabled`, `value`/`defaultValue`/`onChange`, `type`,\n`name`, ...). The forwarded `ref` reaches the underlying `<input>`.\n\nStyling consumes the `--ds-field-*` component-token namespace, which is\nSHARED by TextField, TextArea, and Select by design: one customer override\nretunes every form field consistently.",
      "racBase": "TextField",
      "racPropsNote": "Commonly needed props inherited from the react-aria-components base; the full react-aria-components TextFieldProps surface is also legal.",
      "racProps": [
        {
          "name": "autoFocus",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether the element should receive focus on render."
        },
        {
          "name": "defaultValue",
          "type": "string | null",
          "required": false,
          "defaultValue": null,
          "description": "The default value (uncontrolled)."
        },
        {
          "name": "isDisabled",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether the input is disabled."
        },
        {
          "name": "isInvalid",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether the value is invalid."
        },
        {
          "name": "isReadOnly",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether the input can be selected but not changed by the user."
        },
        {
          "name": "isRequired",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether user input is required on the input before form submission."
        },
        {
          "name": "name",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "The name of the input element, used when submitting an HTML form. See\n[MDN](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input#htmlattrdefname)."
        },
        {
          "name": "onChange",
          "type": "(value: string) => void",
          "required": false,
          "defaultValue": null,
          "description": "Handler that is called when the value changes."
        },
        {
          "name": "value",
          "type": "string | null",
          "required": false,
          "defaultValue": null,
          "description": "The current value (controlled)."
        }
      ],
      "tokenPrefix": "field",
      "storyFile": "packages/react/src/components/TextField/TextField.stories.tsx",
      "example": "<TextField label=\"Email\" type=\"email\" placeholder=\"you@example.com\" />\n<TextField\n  label=\"Display name\"\n  description=\"Shown on your public profile.\"\n/>\n<TextField\n  label=\"Email\"\n  isRequired\n  isInvalid\n  errorMessage=\"Enter a valid email address.\"\n/>",
      "props": [
        {
          "name": "className",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Additional CSS class appended after the component's own classes on the\nfield root. Narrowed from the react-aria-components render-prop form to\na plain string; inline `style` is intentionally not supported (token\nrule)."
        },
        {
          "name": "description",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Optional helper text rendered below the input as a RAC `Text` with\n`slot=\"description\"`, linked to the input via `aria-describedby`."
        },
        {
          "name": "errorMessage",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Error text rendered in a RAC `FieldError` below the input. It is only\nshown while the field is invalid (`isInvalid` or failed validation) and\nis linked to the input via `aria-describedby`."
        },
        {
          "name": "label",
          "type": "string",
          "required": true,
          "defaultValue": null,
          "description": "Visible label for the field. Required: the label is rendered as a RAC\n`Label` and associated with the input automatically, so the field is\nalways accessibly named."
        },
        {
          "name": "placeholder",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Placeholder text shown inside the input while it is empty. Forwarded to\nthe inner RAC `Input` (react-aria-components moves `placeholder` off the\n`TextField` root). Not a substitute for `label`."
        }
      ]
    },
    {
      "name": "Tooltip",
      "description": "Contextual text label shown near a trigger element on hover or keyboard\nfocus, built on react-aria-components `TooltipTrigger` + `Tooltip`.\nOpen/close timing, positioning, flipping, and the `role=\"tooltip\"` /\n`aria-describedby` wiring come from React Aria; visuals are driven purely\nby RAC data attributes (`data-placement`, `data-entering`, `data-exiting`)\nand `--ds-*` tokens (inverse-surface recipe via the `--ds-tooltip-*`\nhooks). Content is plain text only — tooltips never hold interactive\ncontent.",
      "racBase": "Tooltip",
      "racPropsNote": "Commonly needed props inherited from the react-aria-components base; the full react-aria-components TooltipProps surface is also legal.",
      "racProps": [
        {
          "name": "defaultOpen",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether the overlay is open by default (uncontrolled)."
        },
        {
          "name": "isOpen",
          "type": "boolean",
          "required": false,
          "defaultValue": null,
          "description": "Whether the element is rendered."
        },
        {
          "name": "onOpenChange",
          "type": "(isOpen: boolean) => void",
          "required": false,
          "defaultValue": null,
          "description": "Handler that is called when the overlay's open state changes."
        }
      ],
      "tokenPrefix": "tooltip",
      "storyFile": "packages/react/src/components/Tooltip/Tooltip.stories.tsx",
      "example": "<Tooltip content=\"Save your changes\">\n  <Button onPress={save}>Save</Button>\n</Tooltip>\n<Tooltip content=\"Delete project\" placement=\"bottom\" delay={500}>\n  <Button variant=\"ghost\" aria-label=\"Delete\">🗑</Button>\n</Tooltip>",
      "props": [
        {
          "name": "children",
          "type": "ReactElement<unknown, string | JSXElementConstructor<any>>",
          "required": true,
          "defaultValue": null,
          "description": "The trigger element the tooltip describes. Must be a focusable,\nhoverable element (e.g. `Button`) — React Aria attaches hover *and*\nfocus triggering to it, which keyboard accessibility requires."
        },
        {
          "name": "className",
          "type": "string",
          "required": false,
          "defaultValue": null,
          "description": "Additional CSS class appended after the component's own classes.\nNarrowed from the react-aria-components render-prop form to a plain\nstring; inline `style` is intentionally not supported (token rule)."
        },
        {
          "name": "content",
          "type": "string",
          "required": true,
          "defaultValue": null,
          "description": "Text shown inside the tooltip bubble. Deliberately typed as `string`,\nnot `ReactNode`: a tooltip vanishes as soon as pointer or focus leaves\nits trigger, so links, buttons, or any other interactive content inside\nit could never be reached by keyboard or assistive technology. The type\nforbids that class of misuse at compile time."
        },
        {
          "name": "delay",
          "type": "number",
          "required": false,
          "defaultValue": "1500",
          "description": "Warmup delay in milliseconds before the tooltip shows on hover.\nShowing on keyboard focus is always immediate. The default is the\nreact-aria warmup delay (no dedicated motion token exists for tooltip\ndelay in the current token set)."
        },
        {
          "name": "placement",
          "type": "\"top\" | \"bottom\" | \"left\" | \"right\"",
          "required": false,
          "defaultValue": "'top'",
          "description": "Which side of the trigger the tooltip appears on. Automatically flips\nto the opposite side when there is not enough room in the viewport."
        }
      ]
    }
  ]
}
